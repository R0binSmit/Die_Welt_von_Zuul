package location;

/**
 * Diese Klasse definiert die Werte, die das Enum annehmen kann. Diese Enums,
 * die Aktionen des Objekts repräsentieren, werden im Landscape-Objekt zusammen
 * mit einem Text-String in einer HashMap gespeichert um bei Bedarf induviduell
 * für jedes Objekt (nicht jede Klasse) ausgegeben zu werden
 *
 */
public enum LandscapeResponse {
	COLLECT_RESPONSE, COLLECTFINISH_RESPONSE, ENTER_RESPONSE, REMOVE_RESPONSE, USE_RESPONSE
}
